{
    
    "buildCommand": "vite build",
    "devCommand": "vite --port $PORT",
    "installCommand": "npm install",
    "outputDirectory": "dist",
    "rewrites": [
    {
        "source": "/(.*)",
        "destination": "/"
    }
    ]
}